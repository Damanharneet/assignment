import Navbar from "../containers/Navbar";
import TopOfferBar from "../containers/TopOfferBar";
const Layout = (props) => {
  return (
    <div>
      <TopOfferBar />
      <Navbar />
    </div>
  );
};
export default Layout;
